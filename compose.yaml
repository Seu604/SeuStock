services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=seu_stock_db'
      - 'POSTGRES_PASSWORD=seu_stock_pass'
      - 'POSTGRES_USER=seu_stock_user'
    ports:
      - '5433:5432'
